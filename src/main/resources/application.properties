spring.application.name=teamly

server.port=${PORT:8080}

jwt.secret=${JWT_SECRET:mysuperduperextremlylongsecretforjwt}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Railway or other DB connection
spring.datasource.url=jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:teamly}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:password}
spring.jpa.hibernate.ddl-auto=update

cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:dnfwhh6bj}
cloudinary.api_key=${CLOUDINARY_API_KEY:835237556675466}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:-Hyd21jda1P3bg3lzAMzexd41uY}

# Mail configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:opabunmidavid@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wpgrspemzwfdwwxi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
